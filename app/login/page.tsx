'use client';

import { FormEvent, useState } from 'react';
import { signInWithEmailAndPassword, createUserWithEmailAndPassword } from 'firebase/auth';
import { LogIn, UserPlus } from 'lucide-react';
import { auth } from '@/lib/firebase';

export default function LoginPage(){const [mode,setMode]=useState<'login'|'signup'>('login');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{if(mode==='login')await signInWithEmailAndPassword(auth,email.trim(),password);else await createUserWithEmailAndPassword(auth,email.trim(),password);window.location.href='/rewards'}catch(err){setError(err instanceof Error?err.message:'Authentication failed.')}finally{setBusy(false)}}return <main className="min-h-screen bg-[#F4F4F1] px-4 py-10"><div className="mx-auto max-w-md rounded-[30px] bg-white p-6 shadow-sm"><p className="text-[9px] font-black uppercase tracking-[.25em] text-[#E1352B]">PrimeHub Rewards</p><h1 className="mt-2 text-2xl font-black">{mode==='login'?'Login to save your points':'Create your rewards account'}</h1><p className="mt-2 text-xs leading-5 text-black/45">Guest rewards remain available without an account. Login keeps your points, vouchers and redemptions linked to you.</p><form onSubmit={submit} className="mt-6 space-y-3"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="w-full rounded-xl border border-black/10 bg-[#F4F4F1] px-4 py-3 text-sm outline-none"/><input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (6+ characters)" className="w-full rounded-xl border border-black/10 bg-[#F4F4F1] px-4 py-3 text-sm outline-none"/>{error&&<p className="rounded-xl bg-[#FFF1F0] p-3 text-xs font-bold text-[#E1352B]">{error}</p>}<button disabled={busy} className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#14140F] py-3 text-xs font-black text-white disabled:opacity-50">{mode==='login'?<LogIn size={15}/>:<UserPlus size={15}/>} {busy?'Please wait...':mode==='login'?'Login':'Create Account'}</button></form><button onClick={()=>setMode(mode==='login'?'signup':'login')} className="mt-4 w-full text-center text-[10px] font-black text-[#E1352B]">{mode==='login'?"Don't have an account? Create one":"Already have an account? Login"}</button><a href="/rewards" className="mt-4 block text-center text-[10px] font-bold text-black/40">Continue as Guest</a></div></main>}
