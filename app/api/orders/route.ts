import { NextResponse } from 'next/server';
import { getAdminDb } from '@/lib/firebaseAdmin';
export const runtime='nodejs';
const WEEKDAYS=['sunday','monday','tuesday','wednesday','thursday','friday','saturday'] as const; type Weekday=(typeof WEEKDAYS)[number];
type IncomingItem={id?:string|number;productId?:string;name?:string;title?:string;price?:number;originalPrice?:number;image?:string;imageUrl?:string;qty?:number;quantity?:number;dealDay?:Weekday;variant?:{color?:string;size?:string}};
type Customer={name:string;phone:string;email?:string;address:string;city:string;notes?:string};
function pakistanWeekday():Weekday{const day=new Intl.DateTimeFormat('en-US',{timeZone:'Asia/Karachi',weekday:'long'}).format(new Date()).toLowerCase();return(WEEKDAYS.includes(day as Weekday)?day:'sunday') as Weekday;}
function numberValue(value:unknown){const parsed=Number(value);return Number.isFinite(parsed)?parsed:0;}
function variantValue(row:any,key:'color'|'size'){const direct=row?.[key]??row?.[`variant${key[0].toUpperCase()}${key.slice(1)}`];if(typeof direct==='string'||typeof direct==='number')return String(direct).trim();if(row?.options&&typeof row.options==='object'){const value=row.options[key];if(typeof value==='string'||typeof value==='number')return String(value).trim();}return '';}
function findVariant(product:any,item:IncomingItem){const rows=[...(Array.isArray(product.variantMatrix)?product.variantMatrix:[]),...(Array.isArray(product.variants)?product.variants:[])];if(!rows.length)return null;const variant=item.variant||{};const wantedColor=variant.color?String(variant.color).trim():'';const wantedSize=variant.size?String(variant.size).trim():'';const row:any=rows.find((r:any)=>(!wantedColor||variantValue(r,'color')===wantedColor)&&(!wantedSize||variantValue(r,'size')===wantedSize));if(!row){console.error('[checkout-validation] Variant not found',{productId:product.id||product.productId,title:product.title||product.name,itemId:item.id,itemProductId:item.productId,requestedVariant:variant,availableVariants:rows.map((r:any)=>({id:r?.id,color:variantValue(r,'color'),size:variantValue(r,'size'),stock:r?.stock,price:r?.price}))});throw new Error(`${product.title||product.name||'Product'} variant is no longer available.`);}return row;}
async function buildAuthoritativeItems(items:IncomingItem[]){const adminDb=getAdminDb(),currentDay=pakistanWeekday(),settingsSnap=await adminDb.collection('settings').doc('main').get(),settings=settingsSnap.exists?settingsSnap.data()||{}:{},weeklyDeals=Array.isArray(settings.weeklyDeals)?settings.weeklyDeals:[],liveDeal=weeklyDeals.find((deal:any)=>deal?.day===currentDay&&deal?.active!==false&&typeof deal?.productId==='string'&&numberValue(deal?.dealPrice)>0),uniqueIds=[...new Set(items.map(item=>String(item.productId||item.id||'')).filter(Boolean))];if(!uniqueIds.length)throw new Error('Cart does not contain valid product IDs.');const productDocs=await Promise.all(uniqueIds.map(id=>adminDb.collection('products').doc(id).get())),products=new Map(productDocs.map(snap=>[snap.id,snap]));return items.map(item=>{const productId=String(item.productId||item.id||''),snap=products.get(productId);if(!snap?.exists){console.error('[checkout-validation] Product not found',{productId,itemId:item.id,itemProductId:item.productId});throw new Error(`Product ${productId} is no longer available.`);}const product={id:snap.id,...(snap.data()||{})},variant:any=findVariant(product,item),rawStock=(variant as any)?.stock??product.stock??product.quantity,stockProvided=rawStock!==undefined&&rawStock!==null&&rawStock!=='',stock=numberValue(rawStock);console.log('[checkout-validation] Product validated',{productId,documentId:snap.id,title:product.title||product.name,requestedVariant:item.variant||null,matchedVariant:variant?{id:variant.id,color:variantValue(variant,'color'),size:variantValue(variant,'size'),stock:(variant as any).stock,price:(variant as any).price}:null,productStock:product.stock,productQuantity:product.quantity,resolvedStock:stock,stockProvided});if(stockProvided&&stock<=0){console.error('[checkout-validation] Stock unavailable',{productId,itemId:item.id,variant:item.variant||null,rawStock,stock});throw new Error(`${product.title||product.name||'This product'} is currently unavailable for the selected variant.`);}const regularPrice=numberValue(product.price),isLiveDealItem=item.dealDay===currentDay&&liveDeal?.productId===productId,authoritativePrice=(variant as any)&&numberValue((variant as any).price)>0?numberValue((variant as any).price):(isLiveDealItem?numberValue(liveDeal.dealPrice):regularPrice);if(authoritativePrice<=0)throw new Error('Product price is not available.');const quantity=Math.max(1,Math.min(50,Math.floor(numberValue(item.quantity??item.qty??1)))),title=String(product.title||product.name||item.title||item.name||'Product'),image=String((variant as any)?.imageUrl||product.imageUrl||product.image||item.imageUrl||item.image||''),originalPrice=numberValue(product.originalPrice)>regularPrice?numberValue(product.originalPrice):regularPrice;return{productId,title,price:authoritativePrice,originalPrice,quantity,image,variant:item.variant||null,weeklyDealDay:isLiveDealItem?currentDay:null};});}
export async function POST(request:Request){try{const body=await request.json(),rawItems=Array.isArray(body?.items)?body.items as IncomingItem[]:[],quoteOnly=body?.mode==='quote';if(!rawItems.length||rawItems.length>50)return NextResponse.json({error:'Your cart is empty or contains too many items.'},{status:400});const items=await buildAuthoritativeItems(rawItems),totalItems=items.reduce((s,i)=>s+i.quantity,0),subtotal=items.reduce((s,i)=>s+i.price*i.quantity,0);if(quoteOnly)return NextResponse.json({items,subtotal,totalItems});const customer=body?.customer as Customer|undefined;if(!customer?.name?.trim()||!customer?.phone?.trim()||!customer?.address?.trim()||!customer?.city?.trim())return NextResponse.json({error:'Name, phone, address and city are required.'},{status:400});const orderRef=await getAdminDb().collection('orders').add({customer:{name:customer.name.trim(),phone:customer.phone.trim(),email:String(customer.email||'').trim(),address:customer.address.trim(),city:customer.city.trim(),notes:String(customer.notes||'').trim()},items,totalItems,subtotal,total:subtotal,currency:'PKR',status:'pending',source:'website',createdAt:new Date()});return NextResponse.json({orderId:orderRef.id,items,subtotal,totalItems});}catch(error){console.error('Secure order creation failed:',error);return NextResponse.json({error:error instanceof Error?error.message:'Unable to place order.'},{status:400});}}
