name: Salar selected image vision and gallery fix

on:
  push:
    branches:
      - fix/salar-instruction-first
    paths:
      - scripts/salar-selected-vision-gallery-fix.mjs
      - .github/workflows/salar-selected-vision-gallery-fix.yml

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout active branch
        uses: actions/checkout@v4
        with:
          ref: fix/salar-instruction-first
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply selected image vision and gallery patch
        run: node scripts/salar-selected-vision-gallery-fix.mjs

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Commit tested changes to active branch
        run: |
          git config user.name "PrimeHub Salar Patch"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix(salar): inspect selected product images for colour questions"
          git push origin HEAD:fix/salar-instruction-first
