'use client';
import { Check, MessageCircle, ShoppingCart, Zap } from 'lucide-react';
type Props = { stock: number; currentPrice: number; liveDeal: boolean; added: boolean; onOrderNow: () => void; onAddProduct: () => void; onWhatsApp: () => void };
export default function ProductActions({ stock, currentPrice, liveDeal, added, onOrderNow, onAddProduct, onWhatsApp }: Props) { return <div className="mt-4 grid gap-2.5"><button type="button" onClick={onOrderNow} disabled={stock === 0 || currentPrice <= 0} className={`live-deal-cta flex items-center justify-center gap-2 rounded-2xl bg-[#E1352B] py-4 text-xs font-black text-white shadow-lg shadow-[#E1352B]/20 transition hover:brightness-95 active:scale-[0.99] disabled:cursor-not-allowed disabled:opacity-45 ${liveDeal ? 'live-deal-cta-glow' : ''}`}><Zap size={16} fill="currentColor" />{stock === 0 ? 'Out of Stock' : liveDeal ? '🎉 CLAIM DEAL NOW' : 'BUY NOW / ORDER NOW'}</button><button type="button" onClick={onAddProduct} disabled={stock === 0 || currentPrice <= 0} className="flex items-center justify-center gap-2 rounded-2xl bg-[#14140F] py-4 text-xs font-black text-white transition hover:bg-[#0F6A5F] active:scale-[0.99] disabled:cursor-not-allowed disabled:opacity-45">{added ? <Check size={16} /> : <ShoppingCart size={16} />}{added ? 'Added to Cart' : 'ADD TO CART'}</button><button type="button" onClick={onWhatsApp} disabled={stock === 0 || currentPrice <= 0} className="flex items-center justify-center gap-2 rounded-2xl border border-[#0F6A5F]/20 bg-[#0F6A5F]/8 py-4 text-xs font-black text-[#0F6A5F] transition hover:bg-[#0F6A5F]/15 active:scale-[0.99] disabled:cursor-not-allowed disabled:opacity-45"><MessageCircle size={16} />Order on WhatsApp</button></div>; }
