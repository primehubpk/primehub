'use client';
import { Sparkles, ChevronLeft, ChevronRight, Play } from 'lucide-react';
import { titleOf, videoOf, type Product } from './ProductDetailTypes';
type Props = { product: Product; images: string[]; activeImage: number; savingsPercent: number; liveDeal: boolean; onImageChange: (updater: (index: number) => number) => void; onVideoOpen: () => void };
export default function ProductHero({ product, images, activeImage, savingsPercent, liveDeal, onImageChange, onVideoOpen }: Props) {
  const displayImage = images[activeImage] || '';
  return <section className="overflow-hidden rounded-[30px] border border-black/7 bg-white shadow-sm md:sticky md:top-[66px] md:self-start"><div className="relative aspect-square overflow-hidden bg-[#F4F4F1] md:aspect-[4/3]">{displayImage ? <img src={displayImage} alt={titleOf(product)} className="h-full w-full object-cover" /> : <div className="flex h-full items-center justify-center text-sm font-bold text-black/25">No product image</div>}<div className="absolute left-3 top-3 flex flex-wrap gap-1.5">{savingsPercent > 0 && <span className="rounded-full bg-[#E1352B] px-2.5 py-1.5 text-[10px] font-black text-white">SAVE {savingsPercent}%</span>}{liveDeal && <span className="flex items-center gap-1 rounded-full bg-[#0F6A5F] px-2.5 py-1.5 text-[10px] font-black text-white"><Sparkles size={10} />LIVE TODAY</span>}</div>{images.length > 1 && <><button type="button" onClick={() => onImageChange((i) => (i - 1 + images.length) % images.length)} className="absolute left-3 top-1/2 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-white/92 shadow-md" aria-label="Previous image"><ChevronLeft size={18} /></button><button type="button" onClick={() => onImageChange((i) => (i + 1) % images.length)} className="absolute right-3 top-1/2 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-white/92 shadow-md" aria-label="Next image"><ChevronRight size={18} /></button></>}{videoOf(product) && <button type="button" onClick={onVideoOpen} className="absolute bottom-3 left-3 flex items-center gap-1.5 rounded-full bg-white/95 px-3 py-2 text-[10px] font-black shadow-md"><Play size={11} fill="currentColor" />Watch Reel</button>}</div>{images.length > 1 && <div className="flex gap-2 overflow-x-auto p-3">{images.map((image, index) => <button type="button" key={`${image}-${index}`} onClick={() => onImageChange(() => index)} className={`h-16 w-16 shrink-0 overflow-hidden rounded-xl border-2 ${index === activeImage ? 'border-[#E1352B]' : 'border-transparent'}`} aria-label={`View image ${index + 1}`}><img src={image} alt="" className="h-full w-full object-cover" /></button>)}</div>}</section>;
}
