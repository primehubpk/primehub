'use client';
import { Minus, Plus } from 'lucide-react';
import ProductActions from './ProductActions';
type Props = { quantity: number; maxQuantity?: number; stock: number; currentPrice: number; liveDeal: boolean; added: boolean; onQuantityChange: (value: number) => void; onOrderNow: () => void; onAddProduct: () => void; onWhatsApp: () => void };
export default function ProductPurchasePanel({ quantity, maxQuantity, stock, currentPrice, liveDeal, added, onQuantityChange, onOrderNow, onAddProduct, onWhatsApp }: Props) { return <><div className="mt-5 flex items-center justify-between rounded-2xl border border-black/8 p-2"><span className="pl-2 text-[10px] font-black uppercase tracking-wider text-black/40">Quantity</span><div className="flex items-center rounded-xl bg-[#F4F4F1]"><button type="button" onClick={() => onQuantityChange(Math.max(1, quantity - 1))} className="flex h-10 w-10 items-center justify-center" aria-label="Decrease quantity"><Minus size={14} /></button><span className="w-8 text-center text-xs font-black">{quantity}</span><button type="button" onClick={() => onQuantityChange(maxQuantity ? Math.min(maxQuantity, quantity + 1) : quantity + 1)} className="flex h-10 w-10 items-center justify-center" aria-label="Increase quantity"><Plus size={14} /></button></div></div><ProductActions stock={stock} currentPrice={currentPrice} liveDeal={liveDeal} added={added} onOrderNow={onOrderNow} onAddProduct={onAddProduct} onWhatsApp={onWhatsApp} /><p className="mt-3 text-center text-[9px] font-bold leading-4 text-black/35">Secure checkout • Cash on Delivery • Fast delivery options available</p></>; }
