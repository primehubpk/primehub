import CatalogProductCard from './CatalogProductCard';
import { Product } from './ShopTypes';
type Props={products:Product[];addedId:string|null;addProduct:(product:Product)=>void;loading?:boolean;dense?:boolean};
export default function CatalogProductGrid({products,addedId,addProduct,loading,dense=false}:Props){const grid=dense?'grid grid-cols-3 gap-2 md:grid-cols-4':'grid grid-cols-2 gap-3 md:grid-cols-4';if(loading)return <div className={grid}>{Array.from({length:6}).map((_,i)=><div key={i} className="aspect-square animate-pulse rounded-2xl bg-white"/>)}</div>;if(!products.length)return <div className="rounded-[28px] bg-white p-10 text-center"><h2 className="font-black">No products found</h2></div>;return <div className={grid}>{products.map((p,index)=><CatalogProductCard key={p.id} product={p} addedId={addedId} addProduct={addProduct} dense={dense} priority={index<8}/>)}</div>}
