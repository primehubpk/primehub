import Link from 'next/link';
import type { Category } from './ShopTypes';
export default function CompactCategoryStrip({categories}:{categories:Category[]}){const active=categories.filter(c=>c.active!==false);return <section className="mt-4"><div className="mb-3 flex items-center justify-between"><h2 className="text-sm font-black">Shop by category</h2><span className="text-[8px] font-black uppercase tracking-wider text-black/35">{active.length} categories</span></div><div className="grid grid-cols-4 gap-x-2 gap-y-3 sm:grid-cols-6 md:grid-cols-8">{active.map(c=>{const image=c.imageUrl||c.iconUrl;return <Link key={c.id} href={'/category/'+(c.slug||c.id)} className="min-w-0 text-center"><span className="mx-auto flex aspect-square w-full max-w-[64px] overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-black/5">{image?<img src={image} alt={c.title} className="h-full w-full object-cover"/>:<span className="m-auto text-lg">✨</span>}</span><span className="mt-1.5 block truncate text-[9px] font-black">{c.title}</span></Link>})}</div></section>}