import { ChevronRight } from 'lucide-react';

type Props = { buckets: any[]; maxPrice: string; setMaxPrice: (value: string) => void };
export default function BudgetBuckets({ buckets, maxPrice, setMaxPrice }: Props) {
  return <section className="mt-5"><div className="mb-3 flex items-end justify-between"><div><p className="text-[9px] font-black uppercase tracking-[0.2em] text-[#0F6A5F]">Smart shopping</p><h2 className="mt-0.5 text-base font-black text-[#14140F]">Shop by budget</h2></div>{maxPrice !== 'all' && <button type="button" onClick={() => setMaxPrice('all')} className="rounded-full bg-white px-3 py-1.5 text-[9px] font-black text-[#0F6A5F] shadow-sm ring-1 ring-black/5">Clear</button>}</div>
    <div className="flex gap-2.5 overflow-x-auto pb-1 [scrollbar-width:none]">{buckets.map((bucket) => { const selected = maxPrice === String(bucket.amount); return <button key={bucket.id} type="button" onClick={() => setMaxPrice(selected ? 'all' : String(bucket.amount))} className={`relative min-w-[150px] shrink-0 overflow-hidden rounded-[22px] border p-3.5 text-left transition active:scale-[0.98] ${selected ? 'border-[#0F6A5F] bg-[#0F6A5F] text-white shadow-[0_14px_34px_rgba(15,106,95,0.18)]' : 'border-black/6 bg-white text-[#14140F] shadow-[0_10px_28px_rgba(20,20,15,0.06)]'}`}><div className="flex items-center justify-between"><span className={`rounded-full px-2 py-1 text-[8px] font-black uppercase tracking-wider ${selected ? 'bg-white/12 text-white' : 'bg-[#FFF4F2] text-[#E1352B]'}`}>{bucket.title}</span><span className={`text-[9px] font-black ${selected ? 'text-white/70' : 'text-black/35'}`}>≤ Rs. {Number(bucket.amount).toLocaleString()}</span></div><div className="mt-4 flex items-end justify-between gap-3"><div><p className={`text-[9px] font-bold ${selected ? 'text-white/60' : 'text-black/40'}`}>Best finds</p><p className="mt-0.5 text-lg font-black">Rs. {Number(bucket.amount).toLocaleString()}</p></div><ChevronRight className={`h-5 w-5 ${selected ? 'text-white/70' : 'text-black/20'}`} /></div></button>; })}</div>
  </section>;
}
