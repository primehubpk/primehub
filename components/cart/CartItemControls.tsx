'use client';
import { Minus, Plus, Trash2 } from 'lucide-react';
export default function CartItemControls({id,qty,title,removeItem,updateQty,dark=false}:{id:string;qty:number;title:string;removeItem:(id:string)=>void;updateQty:(id:string,q:number)=>void;dark?:boolean}){return <div className="flex items-center gap-2"><div className={`flex items-center rounded-xl ${dark?'bg-white/10':'bg-[#F4F4F1]'}`}><button type="button" onClick={()=>qty<=1?removeItem(id):updateQty(id,qty-1)} className="flex h-8 w-8 items-center justify-center" aria-label={`Decrease ${title} quantity`}><Minus size={13}/></button><span className="w-7 text-center text-[11px] font-black">{qty}</span><button type="button" onClick={()=>updateQty(id,qty+1)} className="flex h-8 w-8 items-center justify-center" aria-label={`Increase ${title} quantity`}><Plus size={13}/></button></div><button type="button" onClick={()=>removeItem(id)} className={`flex h-8 w-8 items-center justify-center rounded-xl ${dark?'text-white/40 hover:bg-[#E1352B]/20 hover:text-[#ff7168]':'text-black/35 hover:bg-[#E1352B]/10 hover:text-[#E1352B]'}`} aria-label={`Remove ${title}`}><Trash2 size={14}/></button></div>}
