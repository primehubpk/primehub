'use client';
import { imageOf, priceOf, titleOf } from '@/components/checkout/useCheckout';
import CartItemControls from './CartItemControls';
export default function CartItem({item,removeItem,updateQty}:{item:any;removeItem:(id:string)=>void;updateQty:(id:string,q:number)=>void}){const qty=Number(item.quantity||item.qty||1),id=String(item.id||item.productId),title=titleOf(item),image=imageOf(item),price=priceOf(item);return <div className="rounded-[22px] border border-black/7 bg-white p-3 shadow-sm"><div className="flex gap-3"><div className="h-20 w-20 shrink-0 overflow-hidden rounded-2xl bg-[#F4F4F1]">{image&&<img src={image} alt={title} className="h-full w-full object-cover"/>}</div><div className="min-w-0 flex-1"><p className="line-clamp-2 text-xs font-black text-[#14140F]">{title}</p><p className="mt-1 font-[family-name:var(--font-mono)] text-sm font-black text-[#E1352B]">Rs. {price.toLocaleString()}</p><div className="mt-2"><CartItemControls id={id} qty={qty} title={title} removeItem={removeItem} updateQty={updateQty}/></div></div></div></div>}
