'use client';
import { Eye, EyeOff, KeyRound } from 'lucide-react';
export default function SettingsPassword({currentPassword,newPassword,showNewPassword,changingPassword,setCurrentPassword,setNewPassword,setShowNewPassword,changeAdminPassword}:{currentPassword:string;newPassword:string;showNewPassword:boolean;changingPassword:boolean;setCurrentPassword:(v:string)=>void;setNewPassword:(v:string)=>void;setShowNewPassword:(v:boolean)=>void;changeAdminPassword:(e:React.FormEvent<HTMLFormElement>)=>Promise<void>}){return <div className="mt-5 rounded-2xl border border-black/10 bg-white p-5"><div className="flex items-start gap-3"><div className="rounded-xl bg-[#14140F] p-2 text-white"><KeyRound size={16}/></div><div><p className="text-sm font-black">Admin Password</p><p className="mt-1 text-[11px] text-black/45">Change the Firebase admin password securely. The current password is never stored in the site settings.</p></div></div><form onSubmit={changeAdminPassword} className="mt-4 grid gap-3 md:grid-cols-3"><label className="grid gap-1 text-xs font-semibold">Current password<input required type="password" autoComplete="current-password" value={currentPassword} onChange={e=>setCurrentPassword(e.target.value)} placeholder="Current password" className="rounded-lg border border-black/15 px-3 py-2 text-sm"/></label><label className="grid gap-1 text-xs font-semibold">New password<div className="flex rounded-lg border border-black/15 px-3"><input required minLength={8} type={showNewPassword?'text':'password'} autoComplete="new-password" value={newPassword} onChange={e=>setNewPassword(e.target.value)} placeholder="Minimum 8 characters" className="w-full py-2 text-sm outline-none"/><button type="button" onClick={()=>setShowNewPassword(!showNewPassword)} aria-label="Show or hide new password" className="text-black/40">{showNewPassword?<EyeOff size={15}/>:<Eye size={15}/>}</button></div></label><button disabled={changingPassword} className="self-end rounded-lg bg-[#14140F] px-4 py-2.5 text-xs font-black text-white disabled:opacity-50">{changingPassword?'Changing...':'Change admin password'}</button></form></div>}
