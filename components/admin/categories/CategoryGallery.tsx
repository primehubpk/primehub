'use client';
import { ImagePlus, Search, X } from 'lucide-react';
import type { CategoryGalleryProps } from './CategoryTypes';
export default function CategoryGallery({ showGallery, gallerySearch, galleryTarget, gallery, selectedUrl, uploading, fileInputRef, onClose, onSearchChange, onChoose, onUpload }: CategoryGalleryProps) {
  if (!showGallery) return null;
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><div className="max-h-[85vh] w-full max-w-4xl overflow-auto rounded-3xl bg-[#F4F4F1] p-5"><div className="flex items-center justify-between"><div><h3 className="text-xl font-black">Choose image</h3><p className="text-xs text-black/45">Select from Media Library, or close and upload directly from your phone/computer.</p></div><button type="button" onClick={onClose} className="rounded-full bg-white p-2"><X /></button></div><div className="mt-4 flex items-center gap-2 rounded-xl bg-white px-3 py-2"><Search size={15} /><input value={gallerySearch} onChange={(e) => onSearchChange(e.target.value)} placeholder="Search gallery" className="w-full bg-transparent text-sm outline-none" /></div><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4 lg:grid-cols-6">{gallery.map((asset) => <button key={asset.id} type="button" onClick={() => onChoose(asset.url)} className={`overflow-hidden rounded-xl border-2 bg-white ${selectedUrl === asset.url ? 'border-[#0F6A5F]' : 'border-transparent'}`}><img src={asset.url} alt={asset.name || 'Media'} className="aspect-square w-full object-cover" /><span className="block truncate p-2 text-[10px] font-bold">{asset.name || 'Image'}</span></button>)}</div>{!gallery.length && <p className="mt-6 rounded-xl border border-dashed border-black/10 p-6 text-center text-sm text-black/45">No images in Media Library yet.</p>}<button type="button" disabled={uploading} onClick={onUpload} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-[#14140F] py-3 text-sm font-black text-white disabled:opacity-50"><ImagePlus size={16} /> {uploading ? 'Uploading...' : 'Upload from phone / computer'}</button></div></div>;
}
